// All service widgets have been removed as they are not in use
// Only information widgets (logo, greeting, etc.) are used and they are in src/components/widgets/

const widgets = {};

export default widgets;
